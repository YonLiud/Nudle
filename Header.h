#ifndef HEADER_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define HEADER_H

#include <string>
#include <iostream>
using namespace std;

string PassGen(long length);	//Declarations

#endif
